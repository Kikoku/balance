import React from 'react';

const InputGroup = ({ children }) => (
  <div
    className="input-group"
    style={{
      marginTop: 15,
      marginBottom: 15
    }}
  >
    {children}
  </div>
)

export default InputGroup;
