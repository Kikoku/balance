import React from 'react';
import {
  Container,
  Row
} from '../Grid';
import Icon from '../Icon';

const LeaguesPage = ({children}) => (
  <Container>
    {children}
  </Container>
)

export default LeaguesPage;
